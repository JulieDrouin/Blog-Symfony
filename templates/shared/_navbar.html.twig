<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Blog</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link" href="#">Dashboard</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Tous les articles</a>
				</li>
				<li class="nav-item">
					{% if app.user %}
						<span class="nav-link">Vous êtes connecté: {{ app.user.username }}</span>
					{% endif %}
				</li>
			</ul>
			{# {% if app.user %} #}
				<a type="button" class="btn btn-warning my-2 my-sm-0" href="{{ path('app_logout') }}">Se Deconnecter</a>
			{# {% endif %} #}
			{# {% if app.user is null %} #}
				<a type="button" class="btn btn-primary my-2 my-sm-0" href="{{ path('security_login') }}">Connexion</a>
			{# {% endif %} #}
		</div>
	</div>
</nav>