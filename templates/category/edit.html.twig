{% extends "base.html.twig" %}

{% block title %}
    Modification de la catégorie {{ category.name }}
{% endblock %}

{% block body %}
    <h4>
        Modifier la catégorie {{ category.name }}
    </h4>
    <div class="mt-3">
        <a type="button" class="btn btn-success btn-sm mb-3" href="{{ path('category_dashboard') }}">Retourner au dashboard</a>
        {{ form_start(formView) }}
        {{ form_errors(formView) }}
        <div class="row">
            <div class="col">
                {{ form_row(formView.name) }}
            </div>
            <div class="mt-3">
                <button type="submit" class="btn btn-primary" role="button">
                    <i class="fas fa-save"></i>
                    Modifier
                </button>
            </div>
        </div>
        {{ form_end(formView) }}
    </div>
{% endblock %}